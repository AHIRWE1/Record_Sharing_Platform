/*
Create the main Express server for a healthcare Integrated Patient Record Sharing Platform.

Requirements:
- Use Express
- Enable CORS
- Parse JSON requests
- Load environment variables with dotenv
- Connect route files:
    /api/auth
    /api/patients
    /api/records
- Server should run on port 5000
- Include basic error handling
*/